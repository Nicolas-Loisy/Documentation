# Doc WIP

Sur le projet librairie écrire un fichier .toml pour paramétrer l'installation et les dépendances.

`python -m build`


Sur le projet qui utilisera la lib :

`pip install fichier_lib.tar.gz[grp_dependances1,grp_dependances2,grp_dependances3]`
